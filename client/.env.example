# API base URL (backend when ready)
VITE_API_URL=http://localhost:5000/api/v1

# Socket server (same host as API, no /api/v1 path)
VITE_SOCKET_URL=http://localhost:5000

# Use mock data without backend (true | false). Set false when API is running.
VITE_USE_MOCK=false
